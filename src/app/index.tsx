/**
 * Entrypoint for the react router app,
 * returns a blank page as the auth context will re-reoute the
 * user to the correct path, either the landing page or the home page
 *
 * @author George Bull
 */
import { View } from "react-native";

export default function Main() {
  return <View></View>;
}
